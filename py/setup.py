from setuptools import setup, find_packages
#import git

#repo = git.Repo('.', search_parent_directories=True)
setup(
    name         = 'g4z3',
    description  = "g4z3's tool kit",
    author       = 'g4z3',
    version      = '0.1',
    packages     = ['g4z3kit']
)
